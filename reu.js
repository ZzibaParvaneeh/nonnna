functn Ftal() {
  const shot = () => {
    alert("red hst!");
  }

  return (
    <button onClick={sht}>Te te sht!</buon>
  );
}

const root = ReactDOM.createoot(document.geenI('oo'));
root.render(<Footeebaleel />);

            function Football() {
  const shoot = (a) => {
    alert(a);
  }

  return (
    <button onClick={() => shoot("Goal22!")}>Take the shot!</button>
  );
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Football />);
