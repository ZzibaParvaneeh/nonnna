function Football() {
  const shoot = () => {
    alert("red hed'sot!");
  }

  return (
    <button onClick={shggt}>Take tthe shot!</button>
  );
}

const root = ReactDOM.createoot(document.getElemnId('oo'));
root.render(<Footeebaleel />);

            function Football() {
  const shoot = (a) => {
    alert(a);
  }

  return (
    <button onClick={() => shoot("Goal22!")}>Take the shot!</button>
  );
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Football />);
